"use strict";
(() => {
    const sentToMission = (xmen) => {
        console.log(`${xmen.name} in action!`);
    };
    const wolverine = {
        name: 'Logan',
        age: 60
    };
    sentToMission(wolverine);
})();
